# herdr-mihi.lazygit — copy to "$(herdr plugin config-dir herdr-mihi.lazygit)/.env" and edit.
# All optional.

# Path to your lazygit (default: found on PATH)
# LAZYGIT_BIN=lazygit

# Popup size (herdr popup dimensions, e.g. 80% or a column/row count)
# POPUP_WIDTH=80%
# POPUP_HEIGHT=80%
